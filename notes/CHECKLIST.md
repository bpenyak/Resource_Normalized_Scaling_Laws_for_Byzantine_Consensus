# CHECKLIST.md

Фінальна перевірка перед подачею. Кожен пункт — або `[x]`, або пояснення чому ні.

---

## L. LaTeX / збірка

- [ ] L1. Збірка **тільки `pdflatex`** (двічі, для перехресних посилань)
- [ ] L2. У `.log` немає `Undefined control sequence`
- [ ] L3. У `.log` немає `Citation ... undefined`
- [ ] L4. У `.log` немає `Reference ... undefined`
- [ ] L5. У `.log` немає `Overfull \hbox` > 10 pt
- [ ] L6. Немає жодного `\TODO` у зібраному PDF (перевірити червоні маркери на полях)
- [ ] L7. Немає заглушок `\rule{...}{...}` замість рисунків
- [ ] L8. Усі `\includegraphics` розкоментовані і файли існують у `figures/`
- [ ] L9. Обсяг ≤ 10 сторінок

## R. Джерела

- [ ] R1. ≥ 25 джерел (наразі 36)
- [ ] R2. Кожен `\bibitem` цитований у тексті хоча б раз
- [ ] R3. Кожен DOI / URL відкривається і веде куди треба
- [ ] R4. Немає незаповнених шаблонів на кшталт `Vol.x, No.x, pp.1--18 (2017)`
      **(дефект статті 1 — не повторити)**
- [ ] R5. Самоцитування `peniak2026a` має заповнені том/номер/сторінки
- [ ] R6. Прізвища транслітеровані однаково в усіх записах
- [ ] R7. Немає дубльованих джерел

## S. Структура (дефекти статті 1 — не повторити)

- [ ] S1. Немає **дубльованих заголовків секцій**
      (у статті 1: «4./5. Validation Testbed: Quorum/IBFT Implementation»)
- [ ] S2. Немає **дубльованих блоків Limitations / Future work**
- [ ] S3. Перехресні посилання вказують на **правильні** номери
      (у статті 1: Observation 2 цитувалася як §3.7, а є §3.4;
      Theoretical Constraint — §3.8 замість §3.5; Algorithm 2 — §3.9 замість §3.6)
- [ ] S4. Немає двох таблиць з однаковим номером («Table 1» двічі у статті 1)
- [ ] S5. Нумерація рисунків і таблиць послідовна
- [ ] S6. Кожен рисунок і таблиця згадані в тексті

## M. Математика

- [ ] M1. Кожна теорема має доведення або явне посилання
- [ ] M2. Кожне припущення (`Assumption`) або перевірене, або зазначене в Limitations
- [ ] M3. Немає non-sequitur у виведеннях
      **(дефект статті 1: з `TPS ∝ B/(M·n²) = n^(−2)` виведено `α = +0.5`)**
- [ ] M4. Знак показника узгоджений: `β > 0` означає **спадання** TPS з `n`
- [ ] M5. Усі позначення в `macros.tex` визначені в тексті при першому вживанні
- [ ] M6. `\cc` (не `\c`!) використовується для клієнтської конкурентності —
      `\c` це cedilla-акцент LaTeX
- [ ] M7. Формула `n_eff = n/(1+(k−1)ρ)` узгоджена між секціями 6 і 7
- [ ] M8. `Theorem~\ref{thm:window}` спирається тільки на леми, доведені раніше

## N. Числа

- [ ] N1. **Жодне число не вписане руками в `.tex`** — усе через `numbers.tex`
- [ ] N2. `numbers.tex` перегенерований після останнього прогону аналізу
- [ ] N3. Числа в анотації збігаються з числами в `09_results.tex`
- [ ] N4. Числа в `12_conclusions.tex` збігаються з `09_results.tex`
- [ ] N5. `\resNmaxMeasured` відповідає фактичному максимальному `n` у `data/raw/`

## H. Чесність

- [ ] H1. Клас відмов названий **«omission + duplication»**, а не «Byzantine»
- [ ] H2. Обмеження про зашифрований RLPx зазначене в `11_limitations.tex`
- [ ] H3. Симуляція X6 **не** подана як вимірювання
- [ ] H4. Абсолютні TPS явно виключені зі сфери тверджень (Scope + Limitations)
- [ ] H5. Розбіжність зі статтями 1 і 2 пояснена, а не замовчана
- [ ] H6. Формулювання самокритики нейтральне
      («previously reported positive exponents, including our own»)
- [ ] H7. Застереження щодо е-голосування присутнє
      (`Remark` у `10_case_study.tex`, посилання на `springall2014`,
      `specter2020`, `park2021`)

## D. Дані та відтворюваність

- [ ] D1. Репозиторій публічний, посилання в `Data availability`
- [ ] D2. `data/raw/` містить сирі артефакти прогонів
- [ ] D3. `make all` відтворює `numbers.tex` і `figures/` з `data/raw/`
- [ ] D4. `requirements.txt` фіксує версії
- [ ] D5. Workflow-файл у репозиторії і запускається

## SEC. Безпека

- [ ] SEC1. У репозиторії немає реальних приватних ключів
- [ ] SEC2. Тестові ключі явно позначені як dev/test
- [ ] SEC3. `.gitignore` виключає `*.key`, `data/keys/`
- [ ] SEC4. JSON-RPC не експонується назовні job
- [ ] SEC5. Немає секретів у логах CI

## U. Українська частина

- [ ] U1. `\abstractUkr` вичитаний носієм
- [ ] U2. `\keywordsUkr` відповідає `\keywords`
- [ ] U3. `\udkUkr` збігається з `\udk`
- [ ] U4. Прізвища авторів однакові в EN і UKR блоках
- [ ] U5. `\maketitleUkr` викликаний **після** бібліографії

## A. Метадані

- [ ] A1. `\MSC` коди актуальні (68M14, 68W20, 90C15, 62J02)
- [ ] A2. `\udk` перевірений
- [ ] A3. `\received{}` заповнений реальною датою
- [ ] A4. Афіліація та email правильні
- [ ] A5. Секції Conflict of interest / Funding / Data availability /
      Use of AI / Contributions заповнені
